// import { createSelector } from '@reduxjs/toolkit'
// import { notesSelector } from '@/entities/Note/model/slice/notesSlice'

// export const getNotesTags = createSelector(getNotes, notes => {
//   const allTags: string[] = []
//   notes.forEach(note => allTags.push(...note.tags))
//   return allTags
// })

// export const getNotesTags = createSelector(notesSelector.selectAll, (notes) => {
//   const allTags: string[] = []
//   notes.forEach((note) => allTags.push(...note.tags))
//   return allTags
// })
